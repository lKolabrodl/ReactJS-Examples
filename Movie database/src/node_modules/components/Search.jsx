import React from 'react';

function Search({handleInput, search}) {
  
  return (
    <section className="searchbox-wrapp">
      <input 
      onChange={handleInput}
      onKeyPress={search}
      type="text" 
      className="searchbox" 
      placeholder="search for a movie"
      />
    </section>
  )
}

export default Search;
